{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build cache_warm debug",
            "type": "shell",
            "command": "bazel build //:cache_warm --config=debug",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "build cache_warm release",
            "type": "shell",
            "command": "bazel build //:cache_warm --config=release",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": false
            }
        },
        {
            "label": "build call_once debug",
            "type": "shell",
            "command": "bazel build //:call_once --config-debug",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": false
            }
        }
    ]
}
